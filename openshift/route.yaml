apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: poro-mcp
  labels:
    app: poro-mcp
spec:
  to:
    kind: Service
    name: poro-mcp
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect


