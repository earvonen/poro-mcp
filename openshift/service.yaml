apiVersion: v1
kind: Service
metadata:
  name: poro-mcp
  labels:
    app: poro-mcp
spec:
  selector:
    app: poro-mcp
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

